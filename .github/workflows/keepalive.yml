name: Keep Supabase Awake

on:
  schedule:
    - cron: "0 18 */3 * *"   # every 3 days
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: curl -f "${{ secrets.KEEPALIVE_URL }}"
